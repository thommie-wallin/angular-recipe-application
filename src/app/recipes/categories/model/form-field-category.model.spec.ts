import { FormFieldCategory } from './form-field-category.model';

describe('FormFieldCategory', () => {
  it('should create an instance', () => {
    expect(new FormFieldCategory()).toBeTruthy();
  });
});
